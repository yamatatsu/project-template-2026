# Backend dev server.
PORT=3001

# Database connection. The backend imports @icasu/db, so the DB client reads
# this at runtime. For local dev, point at the docker-compose Postgres.
# Aurora DSQL and the full set of DB_* options are documented in
# packages/db/.env.example.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app_dev
