# Backend
PORT=3001
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app_dev

# Frontend — override the API base URL (defaults to the Vite "/api" proxy in dev)
# VITE_API_URL=http://localhost:3001
