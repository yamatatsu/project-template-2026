packages:
  - 'apps/*'
  - 'packages/*'

# esbuild ships a platform-specific binary via a postinstall script; allow it so
# CDK's NodejsFunction can bundle the API Lambda locally (no Docker needed).
onlyBuiltDependencies:
  - esbuild
