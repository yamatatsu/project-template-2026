/**
 * セッションの絶対寿命（秒）。失効時刻はログイン時に確定し、リフレッシュでは延長しない
 * （スライドさせると実質の寿命上限が IdP のリフレッシュトークン寿命に移り、寿命ポリシーの
 * 在処が 2 か所に割れる）。Cookie の maxAge と DynamoDB の TTL は必ずここから導出し、
 * 30 日を二重定義しない。
 */
export const SESSION_LIFETIME_SECONDS = 30 * 24 * 60 * 60;
